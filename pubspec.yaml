name: flutter_xml_layout_helpers
description: >-
  Helper widgets & classes for the XML Layout for Flutter (Ark7) VSCode
  extension. Maintained fork of flutter_xmllayout_helpers.
version: 0.1.0
homepage: https://github.com/zhoulijun12315/flutter-xml-layout-helpers

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
